name: disaster-robot
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - numpy
  - pyyaml
  - matplotlib
  - opencv
  # PyTorch (YOLO가 의존) - CPU 버전. GPU 쓰려면 pytorch-cuda 추가
  - pytorch
  - torchvision
  - pip:
      - ultralytics
      - PySide6
